package com.feather.common.config;

/**
 * @author feather
 */
public interface TopicMessageDelegate {
    MessageDelegate getDelegate(String topic);

    MessageDelegate getDelegate(String topic, MessageReceiveDelegate receiveDelegate);
}
